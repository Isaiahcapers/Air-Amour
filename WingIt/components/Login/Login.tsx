import React from "react";
import { SafeAreaView, ScrollView } from "react-native";

const LogIn = () => {

    return (
        <SafeAreaView>
        <ScrollView>
            {/* <Text>Test</Text> */}
        </ScrollView>
        </SafeAreaView>
    );
};

export default LogIn;
